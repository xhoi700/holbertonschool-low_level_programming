#include "main.h"
/**
 *puts2 - prints every other character of a string,
 *@str: dtrfuy
 */

void puts_half(char *str)

int i;

for (i = 0; s[i] != '\0'; i++)
	if( i / 2 == 0)
	_putchar(str[i]);
	{
		if
	}