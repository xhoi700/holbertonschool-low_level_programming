#include "main.h"
/**
 * print_last_digit - check the code
 *
 * Returns: the value of the last digit
 */
int print_last_digit(int n)
{
	if (n >= 0)
	{
		return(last_digit);
	}
	else
	{
		return(-last_digit);
	}
}
